{% set level = level | default(1) %}
{% set entries = entries | default(node.menuEntries) %}
<ul class="toctree toctree-level-{{ level }} toctree-length-{{ entries | length }}">
    {% for entry in entries -%}
        {{ renderNode(entry) }}
    {%- endfor %}
</ul>
